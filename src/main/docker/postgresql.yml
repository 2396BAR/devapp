version: '2'
services:
  devapp-postgresql:
    image: postgres:11.4
    # volumes:
    #     - ~/volumes/jhipster/devapp/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=devapp
      - POSTGRES_PASSWORD=
    ports:
      - 5432:5432
